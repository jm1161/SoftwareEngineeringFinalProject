<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand, id: :book_brand %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :book_name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :book_price %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :book_url %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :book_image %>
  </div>

  <div class="field">
    <%= form.label :asin %>
    <%= form.text_field :asin, id: :book_asin %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
